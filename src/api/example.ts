/**
 * This file provides API functions for interacting with the '/example' endpoint.
 *
 * Each function (POST, PATCH, DELETE) follows a similar structure:
 * - Accepts a payload as an argument.
 * - Sends an HTTP request to the '/example' endpoint using the appropriate method.
 * - Returns the result of the HTTP request.
 *
 * Example shown below is for the POST request.
 */

/**
 * Sends a POST request to the '/example' endpoint with the provided payload.
 *
 * @param payload - The data to be sent in the request body.
 * @returns The result of the postRequest call.
 *
 * @remarks
 * The `patch` and `delete` API functions follow the same structure as this function.
 */
// export function exampleApiFunction(payload: any) {
//     return postRequest('/example', payload);
// }

/**
 * The GET follows this structure
 */

// export function useGetExample() {
//   return useSWR<ExampleResponse>(['example'], () => getRequest('/example'));
// }
